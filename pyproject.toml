[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "a-maze-ing"
version = "1.0.0"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["a_maze_ing", "maze_config", "maze_generator", "parser", "visualizer"]
